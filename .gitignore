database/
dist/
node_modules/
*.log
.env
.env.local
.env.*.local
.DS_Store
coverage/
# Vite UI build output and deps (built via npm run build:ui / in Docker)
ui/dist/
ui/node_modules/
# SDK build output (built via npm run build:sdk)
sdk/dist/ui/tsconfig.tsbuildinfo
